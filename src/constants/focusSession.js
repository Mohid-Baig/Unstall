// Default focus session length. This is intentionally modest —
// the app's philosophy is momentum, not deep-work marathons. Premium
// "timer styles" (per the plan) can offer alternate durations later;
// this stays the single free-tier default.
export const FOCUS_DURATION_SECONDS = 10 * 60; // 10 minutes